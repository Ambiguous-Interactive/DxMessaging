{
  "schemaVersion": 1,
  "candidateId": "505-exact-sequential-control",
  "semanticClass": "exact-sequential",
  "ownership": {
    "bus": "The caller retains its existing IMessageBus; the control does not replace or own it.",
    "payloadLifetime": "The caller owns each item until its synchronous public emission returns or throws; no item is retained.",
    "managedReferences": "The control accepts the same managed references as the ordinary public call and stores none.",
    "alignment": "Managed payload layout follows the existing public message type; no native arena is used.",
    "reclamation": "No new holder exists; existing bus and token cleanup remains per ordinary call."
  },
  "topology": {
    "producers": "single",
    "consumers": "single",
    "threadAffinity": "The existing bus owner thread performs each synchronous emission.",
    "playerLoopPhase": "The caller's current phase; the control does not schedule a later phase."
  },
  "ordering": {
    "guarantee": "linearizable-per-item",
    "linearizationPoint": "Each ordinary public emission call is a separate boundary in input order.",
    "routeAmortization": false
  },
  "capacity": {
    "policy": "caller-length",
    "overflow": "none",
    "wait": "none"
  },
  "lifecycle": {
    "reset": "A reset during item n takes effect before item n+1 exactly as in repeated calls.",
    "cancellation": "Only the ordinary per-item cancellation contract applies; no batch cancellation state exists.",
    "disposal": "Disposal during item n follows the public call contract; later items see the disposed state.",
    "exceptions": "The original exception propagates from item n without wrapping; later items are not called.",
    "producerAbandon": "No reservation exists; stopping before an item publishes nothing for that item.",
    "partialPublication": "Completed earlier items remain observable; no rollback or deferred publication occurs."
  },
  "dynamicDifferences": "none",
  "compatibility": {
    "unity": ["2021.3.45f1", "2022.3.45f1", "6000.5.2f1"],
    "backends": ["Mono", "IL2CPP"],
    "platforms": ["Windows", "macOS", "Linux"],
    "packages": []
  },
  "decision": {
    "workload": "Same-route batches of 1, 4, 16, 64, 128, and 256 calls after prewarm; measurement waits for #510 and #512.",
    "thresholdPercent": 20,
    "capabilityException": "none",
    "batchOneOverhead": "pending #512 timing",
    "breakEven": "pending #512 timing",
    "p99": "N/A until #512 effective-sample rules and open-loop load exist",
    "retainedMemory": "pending allocation and holder checks after semantic RED",
    "playerSize": "pending optional prototype build; no production surface is added",
    "buildTime": "pending optional prototype build; no production surface is added",
    "stopRule": "Stop on any per-item trace mismatch or leaked holder; do not promote below the 20% workload gain without a distinct supported capability."
  },
  "evidence": {
    "state": "tested",
    "sourceSha256": "053ba9844b5807b8bb08f9fb0afcef5dfc24205f9ae0f10e0f91d3dfc4f00869",
    "testSourceSha256": "6bea3cde84a06ea31df723190c8421aed7bf04286df5bdd6ceeeeffcbda5c497",
    "profileSha256": null,
    "evidenceSha256": "8b10127d608df5a8ea5e118ee18ed62a2f4f585ece1683424ea7a32c4365b85a",
    "evidenceLocation": ".artifacts/perf-lab/505-exact-sequential-evidence-v2.json (ignored local index of initial differential and ref-semantic evidence; #505 records hashes)"
  }
}
