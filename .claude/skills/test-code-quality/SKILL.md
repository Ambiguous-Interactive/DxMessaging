---
name: test-code-quality
description: "Keeping tests honest: importing production code instead of
  re-implementing it in the test file, bidirectional SYNC notes where
  duplication is unavoidable (PowerShell script plus JS test, browser-only
  code), accurate describe-block and header categorization (missing vs wrong
  type vs empty), eslint-disable-next-line placement, user-facing message
  wording matching real output, cross-library comparison-benchmark parity with
  honest N/A cells and .github/comparison-packages.json as the single source of
  pins, and the three MessageAwareComponent inspector-overlay invariants. Use
  when a test defines its own copy of production logic, when adding a SYNC note
  or a comparison bridge, or when touching the custom editor overlay."
---

<!-- Generated by `npm run llm:index`. Do not edit by hand. -->

Canonical instructions: [`.llm/skills/test-code-quality/SKILL.md`](../../../.llm/skills/test-code-quality/SKILL.md)

Read that file and follow it. Supporting detail is in the sibling `references/` directory.
